spring:
  application:
    name: ranking-service
  datasource:
    url: jdbc:postgresql://localhost:5434/ranking
    username: postgres
    password: ranking123
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  cloud:
    config:
      enabled: false

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true